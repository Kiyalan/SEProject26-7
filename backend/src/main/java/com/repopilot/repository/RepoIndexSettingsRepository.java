package com.repopilot.repository;

import com.repopilot.entity.RepoIndexSettings;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepoIndexSettingsRepository extends JpaRepository<RepoIndexSettings, String> {
}
