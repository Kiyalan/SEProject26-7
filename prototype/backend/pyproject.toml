[project]
name = "repopilot-backend"
version = "0.1.0"
description = "RepoPilot backend API"
readme = "../README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.115.6",
    "uvicorn[standard]==0.34.0",
    "httpx==0.28.1",
    "python-dotenv==1.0.1",
]

[tool.uv]
package = false
